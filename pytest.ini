[pytest]
# Pin an explicit basetemp: with the default temp dir, Windows on the OneDrive-synced
# tree denies unlinking pytest's 'pytest-current' symlink at teardown (WinError 5),
# which crashes an otherwise all-green run. An explicit basetemp skips that symlink.
addopts = --basetemp=.pytest_tmp
testpaths = tests
